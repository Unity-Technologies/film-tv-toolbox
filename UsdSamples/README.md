These samples should be used with the Unity USD plugin. For Unity version higher than
2019.1, they are distributed with the package itself and available for installation
through the PackageManagerUI. 
